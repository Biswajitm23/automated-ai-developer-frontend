# Frontend

Next.js + TypeScript frontend for Employee Leave Management.

See the [root README](../README.md) for setup and run instructions.

```bash
npm install
npm run dev     # http://localhost:3000
npm run lint
npm run build
```

Configuration: copy `.env.example` to `.env.local` and set `NEXT_PUBLIC_API_BASE_URL`.
